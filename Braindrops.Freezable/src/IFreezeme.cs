namespace Braindrops.Freezable
{
    /// <summary>
    /// Marker interface for easier freezing of objects through extension methods. 
    /// Can also use <see cref="Freezer.MakeFreezable(object,System.Type)"/> directly on objects.
    /// </summary>
    public interface IFreezeme
    {
    }
}